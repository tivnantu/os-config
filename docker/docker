localhost本地映射：
docker run -it --name tivnan-nginx \
 -p 9394:80 \
 -v /mnt/dev/dev/LocalhostMapping/localhost-mapping.conf:/etc/nginx/conf.d/default.conf \
 -v /mnt/dev/dev/LocalhostMapping/localhostmapping:/www/localhostmapping \
 -d nginx

smb
sudo docker run -it --name tivnan-smb \
  -p 139:139 -p 445:445 \
  -v /mnt/daily/smb:/root/smb \
  -d dperson/samba -w "admin" -u "admin;admin" -s "smb;/root/smb;yes;yes;yes;admin;admin;admin"
  